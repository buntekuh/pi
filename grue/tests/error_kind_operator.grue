# expect error: operator '<' not valid for two-value kind 'wet'

kind wet: dry, wet

verb tap
    * noun
    "Nothing."

room Room "A room."

    object bucket "A bucket."
        instead of tap:
            if wet < wet:
                say "Somehow drier."
